#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

bool check(ll n, ll k, ll m, ll x) {
  int startingAmount = n / x;
  if (startingAmount <= m) return false;
  else {
    int val = n - x * m;
    
  }
}

int main() {
  ll n, k, m;
  cin >> n >> k >> m;

  ll lo = 0;
  ll hi = n - 1;

  while (lo <= hi) {

  }
  
}