#include <bits/stdc++.h>
using namespace std;

int main() {
  //  ifstream cin("diamond.in");
 //   ofstream cout("diamond.out");
    int N, K;
    cin >> N >> K;
    int A[N];
    for (int i = 0; i < N; ++i) cin >> A[i];
    sort(A, A+N);
}
