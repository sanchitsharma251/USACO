#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream fin("hoofball.in");
    ofstream fout("hoofball.out");

    int N;
    fin >> N;

    vector<int> pos;
    for (int i = 0; i < N; ++i) {
        int position;
        fin >> position;
        pos.push_back(position);
    }


    
}