#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, M, K;

    int socialHierarchy[M];
    int positions[K];

    for (int i = 0; i < M; ++i) {
        int cow;
        cin >> cow;
        socialHierarchy[i] = cow;
    }

    for (int i = 0; i < K; ++i) {
        int cow;
        cin >> cow;
        positions[i] = cow;
    }

    int order[N + 1];
}
