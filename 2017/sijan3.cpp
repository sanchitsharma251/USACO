#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int parse(string s, ll n) {
    if (n < s.size()) {

    }
}

int main() {
    // ifstream cin("cowcode.in");
    // ofstream cout("cowcode.out");
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    ll n;
    cin >> s >> n;
    int length = s.size();
  
}
