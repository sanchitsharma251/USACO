#include <iostream>
#include <algorithm>
#include <cmath>
#include <stack>
#include <queue>
#include <deque>
#include <set>
#include <map>
using namespace std;

#define ll long long;

int main()
{
    int n, k, A;
    cin >> n >> k >> A;

    pair<int, int> S[n];
    for (int i = 0; i < n; ++i) {
        cin >> S[i].first >> S[i].second;
    }

    // Brute force the permutations
}
