#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        string s;
        cin >> s;

        // Determine the lengths of all prefixes
        priority_queue<int> pq;
        
    }
}
