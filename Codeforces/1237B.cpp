#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; ++i) cin >> a[i];
    int b[n];
    for (int i = 0; i < n; ++i) cin >> b[i];

    // I want to check for the number of pairs between two of the same elements
     
    int ans = 0;
    for (int i = 0; i < n; ++i) {
}
