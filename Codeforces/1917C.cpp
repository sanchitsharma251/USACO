#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, k, d;
        cin >> n >> k >> d;
        int A[n];
        for (int i = 0; i < n; ++i) cin >> A[i];

        // Cannot brute force the amount of days (d could be 10^9)
        
    }
}