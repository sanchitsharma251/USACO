#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int n, q;
    cin >> n >> q;
    ll A[n];
    for (int i = 0; i < n; ++i) cin >> A[i];
    ll K[q];
    for (int i = 0; i < q; ++i) cin >> K[i];

    // Simulate for q minutes
    for (int i = 0; i < q; ++i) {
        // Current # of arrows is K[i]
        int currentArrows = K[i];
        /*
              
        */
    }

}