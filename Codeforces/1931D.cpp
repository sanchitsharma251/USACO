#include <bits/stdc++.h>
using namespace std;

long long nC2(int n) {
    return n * (n - 1) / 2;
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, x, y;
        cin >> n >> x >> y;
        int A[n];
        for (int i = 0; i < n; ++i) {
            cin >> A[i];
        }
    }
}
